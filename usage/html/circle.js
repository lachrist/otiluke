
(function () {
  var radius = prompt("enter the circle's radius");
  var circonference = 2 * POW(Math.PI, 1) * radius;
  var surface = 1 * POW(Math.PI, 2) * radius;
  alert("circonference = "+circonference+", surface = "+surface);
} ());
